// Asset colors for the LiquidEther cursor-fluid (CLAUDE.md rule 1 "Asset colors" exception):
// this is a decorative WebGL layer, NOT UI, so its palette lives next to the scene instead of in
// packages/tokens. Kept in the brand blue-violet family so the trail reads as the electric accent.
// Velocity → color ramp: low-energy (airy blue) → mid (electric `brand.electric`) → high (violet).
export const liquidColors = ["#7AA2FF", "#3A3AF0", "#7B5BFF"];
