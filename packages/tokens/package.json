{
  "name": "@groundtruth/tokens",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "TUM design tokens — colors as TS constants + CSS variables. Single source of truth for every color (CLAUDE.md rule 1).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./src/tokens.css"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
