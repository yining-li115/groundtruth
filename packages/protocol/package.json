{
  "name": "@groundtruth/protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared WebSocket message protocol (docs/architecture.md §5). The single source imported by relay, kiosk, and controller so the contract can never drift (CLAUDE.md §6).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
